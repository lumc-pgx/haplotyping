rule tabulate:
    input:
        matches = "haplotyping/matches/{barcode}.matches.json",
        gene = config["GENE"]
    output:
        "haplotyping/tables/{barcode}.matches.txt"
    script:
        "../scripts/haplotype_summary.py"


